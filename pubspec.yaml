name: flutter_redux_complete
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  redux: ^4.0.0
  flutter_redux: ^0.6.0
  copy_with_extension: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ">=1.0.0 <2.0.0"
  copy_with_extension_gen: ^1.0.7

flutter:

  uses-material-design: true
